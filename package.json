{
  "name": "cloud-cap-my-samples",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^3",
    "@sap/cds-odata-v2-adapter-proxy": "^1.4.30",
    "@sap/hana-client": "^2.4.177",
    "express": "^4"
  },
  "scripts": {
    "start": "npx cds run",
    "startv2": "node v2server"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": "sample.db"
        }
      }
    }
  },
  "devDependencies": {
    "sqlite3": "^4.2.0"
  }
}
